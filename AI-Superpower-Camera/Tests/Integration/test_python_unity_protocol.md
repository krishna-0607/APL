# Python to Unity Integration Test

Protocol validation is implemented by `PythonVision/tests/test_integration_ws.py`.
The test verifies that custom power events are accepted and echoed by the backend WebSocket server.
